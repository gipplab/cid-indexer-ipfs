# Secrets and local runtime state (never bake into the image)
.api_key
keyword_index.json
keyword_failures.json
archives.json
submissions.json
denylist.json
settings.json
*.tmp

# Prebuilt binary (rebuilt inside the image)
cidindexer-ipfs

# VCS and docs not needed for the build
.git
.gitignore
README.md
Dockerfile
.dockerignore
