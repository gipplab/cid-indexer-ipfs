# Runtime state directory (DB, API key, archives, moderation state)
data/

# Secrets (also kept under data/ at runtime)
.api_key
.env

# SQLite index database (legacy location / non-default -o dirs)
index.db
index.db-wal
index.db-shm

# build artifacts
cidindexer
cidindexer-ipfs
*.tmp
